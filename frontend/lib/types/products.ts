export interface ProductsResponse {
    id: string;
    name: string;
    price: string;
    description: string;
    inventory: string;
    createdAt: string;
    updatedAt: string;
}

export interface ProductCreateData {
    
}